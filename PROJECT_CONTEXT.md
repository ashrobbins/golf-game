# Beating Bogey — Project Context

A handoff doc for picking this project up in a fresh chat. Paste this in, or point a new session at this file.

## What this is

A golf draft-and-simulate game, inspired by [38-0](https://38-0.app): draft a bag of 18 real golfers (one per hole) via a wheel-spin mechanic, then simulate a round hole-by-hole and chase a bogey-free 18. Original design doc: `golf-draft-game-plan.md` (repo root) — read that for the full original concept, including deferred future ideas (Tour Season, leaderboards, Supabase backend).

**Current scope**: client-side only, no backend, no accounts — Phase A below (local-only stats) has since shipped on top of that, everything else is still ahead. See the pinned section immediately below for the accounts/season roadmap.

## ⏳ Active work: player stats (Phase A ✅ shipped on `main`) and a 16-round Season mode (Phase B/accounts dropped)

This is now the single most important thing for a new session to know. The user originally asked for a
roadmap covering three related future features (stats, Supabase accounts, Season mode); a plan was
researched, refined through several rounds of follow-up questions, and approved. Work happened on a
`stats` branch, PR'd, and **merged into `main` via PR #1 (merge commit `ced2a14`)** — `stats` is now
fully merged and stale; don't branch from it, everything is on `main`. **Phase A is built, manually
verified in the browser, committed, and merged** — see session history items 29–34 below for exactly
what shipped, including several follow-up refinements that went well beyond the original plan's scope.

**Phase B (Supabase accounts) has since been explicitly dropped.** In a later session the user floated
monetizing Season Mode behind a paywall, which was the only reason accounts existed in this roadmap at
all. After a feasibility/legal-risk writeup (real-money payment processing costs, App Store commission
and 4.2 rejection risk, and — more materially — real legal exposure from charging money for a product
built on real golfers' names and real course names, including a documented near-identical precedent of
Augusta National suing a golf simulator over exactly this), **the user decided to keep the game free and
web-only, permanently** — their words: "making money from it was never the original intention." Season
Mode is now planned as fully local-only/localStorage-based (see `sesons-mode.md`'s data model), with no
accounts dependency. Don't resurrect the accounts/monetization angle unless the user explicitly raises
it again.

**Season Mode's course-content blocker is now resolved.** Building Season Mode surfaced that its
16-round schedule needed 4 more real courses than existed; the user chose to build those first rather
than ship an uncompletable season. That's now done — see the "16 courses now" note under
`courses.json` below. Season Mode itself (the actual feature — data model, UI, achievements) is still
fully unbuilt; the user hasn't yet said when to start it.

**The full plan lives outside this repo**, at `/Users/ash.robbins/.claude/plans/ok-so-for-an-crystalline-ladybug.md` — read that file in full before starting any of this work; the summary below is not a substitute for it. **The user has since also committed a copy into the repo directly**, at root-level `phased-feature-development-plan.md` (commit `7d03159`, made by the user outside any session, not by Claude) — same content, now durable in-repo either way.

**Three phases, meant to be built independently, in order:**

1. **Phase A — local stats tracking — ✅ built and committed (`a91e491`, session history #29–30).** Times gone bogey-free, lowest bogeys / highest birdies / highest eagles in a round, career hole-in-ones — tracked both career-wide and per-course. New `src/game/stats/` module (`types.ts` / `storage.ts` / `deriveStats.ts`, mirroring how `draft`/`simulation` split types from engine logic), storing one full `RoundRecord` (a `SimulationResult` plus `id`/`playedAt`) per completed round in `localStorage` under a versioned `{ version: 1, rounds: RoundRecord[] }` shape — **full `holeResults` are stored, not just derived counts**, both for a future round-history UI and so a future Supabase upload is a near-1:1 mapping. Hooks into `GameProvider.tsx`'s `finishDraft()` (right after `setSimulationResult(result)`). Surfaced as **a new dedicated page** (`StatsPage.tsx`, a new `'stats'` value on `GameContext`'s `View` union) with its own nav entry point — explicitly *not* a drawer like `HowToPlayDrawer`, since a round-history table needs more room than a 400px panel. **Grew well beyond the original plan's scope, same session, across several follow-up asks**: a career "Top players" leaderboard (`rankGolfers()` + `PlayerLeaderboard` component, with a per-golfer tier-count breakdown), a Career-vs-per-course visual hierarchy redesign (Career is now the clear focal point; per-course stats collapsed into a compact "By course" list), real per-course country flags, a site-wide footer with golf-flavored disclaimer copy, a "Let's Go" CTA on the empty stats state, and a "Tee off" CTA that replaced the old auto-simulate-on-draft-complete behavior — see session history #29–30 for the full detail on each.
2. **Phase B — Supabase accounts (deferred until Phase C needs them).** Verified current pricing (Free: 500MB DB / 50k MAU / $0, likely sufficient for a long time, but **projects auto-pause after a week idle** — a real risk since seasons play out over days/weeks; Pro is $25/mo and removes the pause). Schema (`profiles` / `rounds` / `hole_results` / `seasons` tables) designed as a direct upload target for Phase A's local data. **Auth: OAuth via Google + Apple** — Apple specifically because the original design doc's planned Capacitor iOS wrapper means App Store review will require Sign-in-with-Apple once any other social login is offered, so building it in from the start avoids a forced rework later.
3. **Phase C — Season mode (16 rounds: 12 regular + 4 majors, local-only — does not need Phase B).** This section was stale
   as of the note below — the shortlist described here (Carnoustie as a major, Southern Hills/Wentworth
   West/Jumeirah Golf Estates as regular stops) was an early, since-superseded proposal, not what was
   actually decided. **The current, authoritative Season Mode design lives at
   `/Users/ash.robbins/.claude/plans/sesons-mode.md`** (design/mockup phase, approved but not yet
   built) — read that file, not this paragraph, for the real plan. Confirmed course list: majors are
   **Augusta National, Pebble Beach, Royal Birkdale, and Pinehurst No. 2**; the other 12 season rounds
   use the remaining 12 non-major courses already in `courses.json` (all 16 minus the 4 majors). **All
   16 courses
   Season Mode needs now have real hole-by-hole content** — `courses.json` was extended from 12 to 16
   courses (Pinehurst No. 2, Whistling Straits, Royal Melbourne, Royal County Down added, scorecard-
   verified via WebSearch against USGA/PGA Tour/official club sources), so the "content isn't built
   yet" blocker that paused Season Mode is now resolved. Season Mode itself (data model, UI, the
   season-tagged `RoundRecord` fields, the Season achievements tab) is still fully unbuilt.

**Next step, whenever this is picked up**: Phase A is built, verified, committed, and merged to `main`.
All 16 courses Season Mode needs are now in `courses.json` with real data. Phase B (Supabase accounts)
was explicitly ruled out by the user — see the note below this list, they've decided to keep the game
free/web-only and are not monetizing it. Season Mode (Phase C) can proceed without accounts, since it
was always designed to be local-only/localStorage-based (see `sesons-mode.md`'s data model) — the
original Phase B dependency assumed a monetized, account-gated version that the user has since decided
against. The user hasn't yet said whether/when to start building Season Mode itself.

---

## Direction B redesign (concluded): reveal/results + draft + home page all shipped. Course-info page never started.

Historical reference for the design system these screens established — not active work anymore, but the Components/Design-system sections below still reference the "3 fixes" and specs here by name, so this hasn't been trimmed down. We ran a design exploration (two directions, mocked as standalone HTML artifacts, not real code) for the results + reveal experience, and **the user chose a direction; the reveal/results stage AND the draft-screen stage of the implementation are done and committed (`99ce1fb`).**

Home/course-info (the plan's stage 3) was explicitly **not** built as part of Direction B. When asked, the user chose to skip it rather than do even the smallest version (just the missing course-card meta line) — their words: they want to **redesign these two pages in detail later**, not retrofit the existing Direction B mockup onto them. That "later" arrived, per session history item 27 below — but it was its own fresh design conversation exactly as predicted here, **not** a continuation of Direction B or its mockup (the home page now actually looks closer to `ResultsHero` than to anything in the Direction B mockup). **Course-info is still genuinely untouched** — the same "check with the user first, don't retrofit the old mockup" guidance still applies there.

- **Chosen direction: "Direction B — Evolved."** Not a visual overhaul — it reuses the app's real, already-shipped tokens (Sora, `--accent` indigo, `--tier-gold/green/red`) and the circle/square/gold-disc scorecard notation already built this session. What changes is confidence: a hero stat card, a proper card for the current-hole reveal instead of a text line, and (new) an 18-dot hole-outcome indicator.
- **Rejected (for now): "Direction A — The Manual Board."** A from-scratch reimagining themed as Augusta's real manual leaderboard (green board, ivory placard lettering, Anton + Space Mono, brass reserved for a hole-in-one), with a neutral graphite theme worked out for non-branded courses. The user liked it but called it too large a bet for now (new fonts, a second full visual system, a whole-site redesign). **Don't resurrect this without the user raising it** — it's parked, not dead.
- **Design artifacts** (private Artifacts, tied to the user's account — accessible from any session, not this specific chat; use `Artifact` tool with `action: "list"` if a link 404s or goes stale):
  - `https://claude.ai/code/artifact/b7b92036-674d-452f-b841-3191ab3fb1a5` — the original two-direction pitch (results/reveal screens only). Superseded by the two full-site mockups below for anything beyond the initial concept.
  - `https://claude.ai/code/artifact/2bda102d-c92f-4cad-b11f-9fbdc36c080f` — **Direction B, full site (the one to build from).** Home, course info, draft, live reveal, final results, all 5 screens, same mock round throughout.
  - `https://claude.ai/code/artifact/d5907ce3-ec06-4027-bab3-8ec433a11abf` — Direction A, full site, parked per above. Kept for reference only.

### The agreed plan (concluded)

Implemented in stages, not one giant change: **reveal/results screens (done)**, then **draft (done)**. Stage 3, home/course-info, was **deliberately skipped** at the user's choice — see the note above. Comparing the current `HomePage`/`CourseCard`/`CoursePreviewPage`/`CourseHoleTable` against the mockup at the time: they already matched closely (front/back split, Out/In/Total subtotals, same par/location display); the only gaps were a missing "18 holes · yardage" meta line on the course cards, and the mockup's dot+plain-label archetype style vs. the app's real `ArchetypeBadge` on the hole table. Neither gap was judged worth a piecemeal fix given a fuller redesign is coming later.

### Three fixes the user flagged on the Direction B mockup — bake these in from the start, don't build the naive version first

1. ✅ **Done — turned out to need no work.** Archetype branding on the draft screen's hole info needed to be more prominent (mockup showed it as plain muted text). Checked `HoleHeader.tsx` before touching anything: it already renders the hole's archetype as a separate, upsized `ArchetypeBadge` (from an earlier session, commit `087e402`), not plain text — confirmed in the browser it already reads clearly, more prominent than the mockup even. No changes made.
2. ✅ **Done.** The 18-dot indicator overflowed the hero card in the first Direction B mockup. Root cause (confirmed): 18 dots at 11px + 6px gap = ~300px, but the hero card's inner content width is ~272px. Implemented at a fixed 9px dot size, but see the **post-review refinement** below — the strip itself is now responsive rather than a fixed total width.
3. ✅ **Done, then generalized further after user review.** Breathing room between the legend-tier gold left edge and the hole number. First pass only added `padding-left` to legend rows (`.legend`), which fixed the padding but left legend and non-legend rows *misaligned* with each other (non-legend row text started further left). User caught this by screenshot. Fixed properly by giving **every** row the same inset `box-shadow` + left padding, in a neutral grey (`--border`) by default, with `.legend` only overriding the *colour* to gold — so the bar's presence never shifts a row's content, legend or not. Applied identically in both `HoleResultRow.module.css` (reveal/results) and `DraftRoster.module.css` (draft roster, "Your bag") — this is now the standard pattern for any legend-tier row in the app, see "Design system" below.

### Dot indicator spec (Direction B) — ✅ implemented (`HoleOutcomeDots.tsx` + `DraftProgressDots.tsx`)

18 small dots, one per hole, replacing what would otherwise be a plain progress bar:
- Not yet played: faint grey (`--border`)
- Par: dark grey — **use `--tier-gold`'s sibling token `--tier-grey`**, which had existed in `index.css` since early in the project but was never actually consumed anywhere until this dot design. Finally gave it a job.
- Birdie or eagle: `--tier-green`
- Bogey or worse: `--tier-red`
- Hole-in-one: `--tier-gold` (confirmed with the user: gold is ace-only, eagle does *not* get its own colour, it's grouped with birdie)

**Post-review refinement**: the first pass sized the strip at a fixed 9px-dot/4px-gap total width, which sat bunched at the left of the hero card instead of filling it. Fixed by keeping the 9px dot size (still needed — it's what keeps 18 dots from overflowing a 320px viewport) but changing the container to `justify-content: space-between`, so the *gaps* stretch to fill however wide the card actually is, rather than the dots themselves resizing.

On the draft screen, the same dot strip (literally the same CSS module, `HoleOutcomeDots.module.css`) is reused via a separate `DraftProgressDots.tsx` component with simpler two-state semantics: filled (`--accent`, `.picked` class) = drafted, grey = not yet — no outcome color needed since nothing's been played yet. Rendered in `DraftRoster.tsx` between the "Your bag N/18" title and the roster table.

### Row-by-row live table spec (Direction B) — ✅ implemented (`HoleResultRow.tsx`, used from `RevealSequence.tsx`)

Underneath the current-hole reveal card, the same per-hole commentary list that's shown on the *final* results screen now also builds up live, one row per hole, as the round plays out — confirmed with the user and built accordingly:
- The table holds **only completed holes**. The hole currently being revealed stays solely in the spotlight/current-hole card (`CurrentHoleCard.tsx`) above; once it settles, a new row animates into the table below (`HoleResultRow`'s `animateIn` prop, a `rowIn` keyframe respecting `prefers-reduced-motion`). No duplication between the two — `RevealSequence.tsx` slices `holeResults` to `revealedCount - 1` for the table, separately from the single current hole at `revealedCount - 1` shown above it.
- `HoleResultRow` is genuinely shared, not just visually similar — both `RevealSequence.tsx` (live, growing) and `Scorecard.tsx` (final, static) render the exact same component, so an outcome always reads identically in both places (same principle as `ScoreMark`).

## Repo & environment

- Path: `/Users/ash.robbins/repos/golf-game`
- GitHub: `https://github.com/ashrobbins/golf-game.git`. **On branch `main`** — the `stats` branch (session history #28–30) was merged in via PR #1 (`ced2a14`) and is now stale; everything since lives directly on `main`. Working tree checked for cleanliness before each commit; `main` is pushed and up to date with `origin/main` as of commit `9863f33`, plus one commit (`7d03159`) the user made directly outside any session (see the pinned section's note on `phased-feature-development-plan.md`).
- Commits since the `stats` merge, landed straight on `main`: `056e0cc` (hide the nav Stats label on narrow screens), `c99feef` (reveal skip button, stats-page layout tidy-up, return-to-home CTA), `846ff32` (local-storage disclosure + the `100svh`→`100dvh` mobile bottom-gap fix), `abe503f` (draft-screen layout-shift fix + skip-button threshold), `24064c0` (PWA support), `0b84c57` (doc catch-up), `34f45b3` (Valderrama + TPC Sawgrass courses, home-card flags), `7d03159` (user's own commit, the roadmap plan file), `9863f33` (course flags threaded through preview/draft/reveal/results) — see session history #31–37 for detail on each.
- Nine commits landed on `main` in an earlier session (`99ce1fb`, `9fb21ac`, `f33970e`, `412c3f5`, `80f1ce2`, `d8cb6dd`, `bbc2224`, `4498fa4`, `61136cc`) — see session history items 17–27 below for what's in each.
- Prior to this session: `bce934f`, `3f503eb`, `1057b70` covering the results-table redesign, share removal, the iOS reel-animation fix, and the course-name/hole-18/`?simReveal` work — see session history below for what's in them.
- This doc is being kept current roughly every 30 minutes during active sessions, per explicit user request — if you're picking this up mid-session, check whether a fresher version exists before treating it as current.
- Stack: Vite + React 19 + TypeScript, CSS Modules (no Tailwind/CSS framework), Vitest for tests. No router — view switching is a plain state enum (see below).
- Font: **Sora** (self-hosted via `@fontsource/sora`), chosen to match a Dribbble reference the user shared for visual identity.
- Run it:
  ```bash
  npm install
  npm run dev      # vite dev server
  npm run test     # vitest
  npm run lint     # eslint
  npm run build    # tsc -b && vite build
  ```

### Debug query params (never touch real game state beyond a one-time initial jump; no backend/persistence exists for them to corrupt anyway)

- **`?simResults`** — jumps straight to the *finished* results page using hand-crafted mock data (`src/content/mockSimulationResult.ts`: real golfers/course, outcome tiers deliberately chosen — not simulated — to cover all 5 tiers at least once, including a hole-in-one, which real random play essentially never produces).
- **`?simReveal`** — same mock data, but lands at the *start* of the live reveal sequence instead of skipping to the end. Added specifically so the reveal/results redesign work above can be checked in seconds instead of playing a full 18-hole draft every time. **Use this constantly while doing the reveal/results work.**
- Both get stripped from the URL on `playAgain()` (`GameProvider.tsx`) so they don't linger/get shared by accident.

## Architecture

**Pure logic is fully decoupled from React/UI**, under `src/game/`:
- `src/game/draft/engine.ts` — pure draft state machine (spin wheel → draw 3 golfers → pick one → repeat). Rules: countries stay on the wheel until either their `repeatCap` picks are made or their remaining bench drops below 3; picked golfers never reappear; unpicked-but-offered golfers return to the bench.
- `src/game/simulation/engine.ts` — given the completed 18-hole bag, resolves an outcome tier per hole (`hole_in_one | eagle | birdie | par | bogey_plus`) from `public/content/odds-config.json`. Archetype fit is a continuous 0–1 blend (`src/game/simulation/affinity.ts` — `golferHoleFitWeight`, via a ranked best-to-worst affinity table per archetype), lerped between the `matched`/`unmatched` distributions rather than a hard either/or switch. `src/game/simulation/skill.ts`'s `applySkillShift` (`legend/elite/solid/journeyman`) is layered on top and scales with that same fit weight. `affinity.ts` also exports `bestFitArchetype`, used purely for display (which of a golfer's archetypes to show as "theirs" on a given hole), not for the odds math.
- `src/game/simulation/commentary.ts` — template-based, golf-accurate flavor text per hole outcome.
- `src/game/rng.ts` — seedable `mulberry32` RNG used in tests for determinism; production code defaults to `Math.random`.
- `src/game/stats/` (new, Phase A — session history #29) — `types.ts` (`RoundRecord`, `StatsStore`), `storage.ts` (`loadStats`/`recordRound`, wrapping every `localStorage` call in try/catch with an in-memory fallback if storage is unavailable), `deriveStats.ts` (pure reducers: `deriveStats`/`deriveStatsForCourse` for the round-level career/per-course cards, plus `rankGolfers` for the career player leaderboard — see below). `GameProvider.tsx`'s `finishDraft()` calls `recordRound(result)` right after `setSimulationResult(result)`; the `?simResults`/`?simReveal` debug shortcuts deliberately bypass `finishDraft()` so they never pollute real stats (verified in the browser: round count stays unchanged after visiting `?simResults`).

**Content is static JSON**, fetched at runtime from `public/content/`:
- `countries.json` — 19 countries, 124 golfers, including a synthetic 19th "Others" country (id `others`, isoCode `OTH`, deliberately falls through to the white-flag emoji default) for real players who don't cleanly fit one curated nation. Each golfer has 1–2 `archetypes` and an optional `skill` tier. A `Country` can carry an optional `repeatCap` (currently only USA/England, both 5) overriding the default max-picks-per-country (`REPEAT_CAP = 3`).
- `courses.json` — 16 courses now, array order = home page display order. The original 12 (Augusta
  National, Carnoustie, Valderrama, Royal Birkdale, TPC Sawgrass, Marco Simone, Pebble Beach, St
  Andrews, Brabazon, Le Golf National, Medinah, Earth Course) were added across earlier sessions; the
  latest 4 (Pinehurst No. 2, Whistling Straits, Royal Melbourne — the Composite Course, and Royal
  County Down) were added specifically to unblock Season Mode's 16-round schedule, since Pinehurst No.
  2 is one of the mode's 4 majors. All 16 are verified against real scorecards (`WebSearch` against
  USGA/PGA Tour/official club sources, not memory) — see `sesons-mode.md` for the sourcing detail on
  the newest 4. Only Augusta/Carnoustie/St Andrews/**Whistling Straits** have real per-hole `name`s —
  every other course deliberately doesn't, since none of them has official names for *every* hole and
  partial naming was judged worse than none.
- `odds-config.json` — per-par-type `matched`/`unmatched` outcome distributions, with non-zero floors enforced in tests so archetype fit never guarantees an outcome.

**State/routing**: `src/state/GameProvider.tsx` + `GameContext.ts` + `useGame.ts`. No react-router — a `View` enum (`'home' | 'course-info' | 'draft' | 'results' | 'stats'`) drives which page renders in `App.tsx`. `viewStats()` (new, Phase A) is a plain `setView('stats')` action, same shape as `beginDraft`/`playAgain` — stats is a real page in the main nav flow, not overlay chrome, so it didn't need a second context the way the rules drawer did. A second, unrelated context lives alongside `GameContext` the same way — `HowToPlayContext.ts` + `HowToPlayProvider.tsx` + `useHowToPlay.ts` — purely UI chrome (is the rules drawer open), deliberately kept separate since it's not game state. Both providers wrap `App.tsx`'s tree independently (order between them doesn't matter, they don't interact).

**Components** under `src/components/`: `draft/`, `picker/`, `scorecard/`, `course/`, `ui/`, `nav/`, `home/`, `stats/` (new, Phase A). **`share/` was deleted earlier this session's history** (see history) — don't recreate it without being asked.

- `ScoreMark` (`src/components/scorecard/ScoreMark.tsx`) — shared golf-scorecard notation (circle=birdie, double-circle=eagle via `border` + offset `outline` rather than a second DOM element, square=bogey+, filled gold disc=hole-in-one, plain text=par). Used by `ScorecardGrid`, `HoleResultRow`, and `CurrentHoleCard`, so an outcome always renders identically everywhere. Takes a `size` prop (`'default' | 'large'`) — `'large'` (32px) is used only by `CurrentHoleCard`'s spotlighted mark.
- `RoundHero` (`src/components/scorecard/RoundHero.tsx`) — the Direction B hero stat card (course name · total-to-par, bogey-free streak chip, `HoleOutcomeDots` strip), leads with the score. **Live-reveal only now** (`RevealSequence.tsx`) — `Scorecard.tsx` (final results) switched to `ResultsHero` instead, see below. Still self-contained: computes its own total/streak from `holeResults.slice(0, revealedCount ?? holeResults.length)`.
- `ResultsHero` (`src/components/scorecard/ResultsHero.tsx`) — results-only hero, deliberately *not* shared with `RoundHero`/the live reveal. A two-panel split: a wider streak panel (the actual goal of the game, so given more weight than the score) beside a narrower "Final Score" panel, `HoleOutcomeDots` spanning the full width underneath. Unlike `RoundHero` it takes the already-computed `totalStrokesToPar`/`bogeyFreeThroughHole`/`isBogeyFreeRound` straight from `SimulationResult` as props rather than recomputing from a `revealedCount` slice — there's no partial-reveal case to support here, the round is always complete by the time this renders. On `isBogeyFreeRound` the whole card washes gold (`color-mix(--tier-gold, ...)`, same token `Confetti`'s trigger and the old chip used) and both panels go transparent so the wash reads through evenly; copy changes to "🏆 Bogey-free for the entire round!" (a deliberately different, more conclusive string than `RoundHero`/`formatBogeyFreeHeadline`'s "Bogey-free 18!" — the two are allowed to diverge since they're now different components serving different moments). Designed via two Artifact mockups, iterated live with the user (session history item 23 has the detail) before being built for real.
- `HoleOutcomeDots` (`src/components/scorecard/HoleOutcomeDots.tsx`) — the 18-dot outcome strip (see spec above), five-colour mode, used by `RoundHero`. Its CSS module (`HoleOutcomeDots.module.css`) is also imported directly by `DraftProgressDots` for the shared `.strip`/`.dot` base styles.
- `DraftProgressDots` (`src/components/draft/DraftProgressDots.tsx`) — the draft screen's two-state variant of the same dot strip (drafted vs. not). Deliberately a separate small component rather than a mode flag on `HoleOutcomeDots`, since the two take different data shapes (outcome tiers vs. a simple drafted count).
- `CurrentHoleCard` (`src/components/scorecard/CurrentHoleCard.tsx`) — the spotlighted current-hole card shown during a live reveal, replacing the old plain-text status line.
- `HoleResultRow` (`src/components/scorecard/HoleResultRow.tsx`) — the shared per-hole row (flag, name, commentary, `ScoreMark`), used by both `RevealSequence.tsx`'s live-growing table and `Scorecard.tsx`'s final list. Has an `animateIn` prop (only ever set `true` by `RevealSequence.tsx`, for the row that's newly landed at the bottom) driving a one-time `rowIn` entrance keyframe that respects `prefers-reduced-motion`. Every row gets a neutral grey inset left edge + matching padding; `.legend` only swaps the colour to gold (see fix #3 above) — don't reintroduce a legend-only edge without the matching base-row treatment.
- `CountryFlag` (`src/components/picker/CountryFlag.tsx`) — wraps `isoToFlagEmoji()` and special-cases Northern Ireland (`GB-NIR`, which has no real Unicode flag sequence) to render an actual image of the Ulster Banner instead of a black-flag fallback. Named `CountryFlag` not `Flag` specifically because `Flag.tsx` collided with the existing `flag.ts` on macOS's case-insensitive filesystem and broke `tsc -b`.
- `GolferCard` (`src/components/draft/GolferCard.tsx`) has exactly one call site (`GolferReels`). Takes an optional `onClick` — when set, the whole card is a keyboard-accessible selection control, not just the Select button below it. Surname text is centered and never mid-word-broken; a `.surnameLong` modifier (10px, tighter tracking) kicks in for any surname with a space/hyphen-delimited segment ≥10 chars (see session history #26) — the card's text column is only ~90–92px wide no matter the viewport, since `GolferReels`' 3-column reel is capped at `max-width: 420px` total, not a percentage.
- `DraftRoster` (`src/components/draft/DraftRoster.tsx`, "Your bag") is a real 3-column CSS grid (golfer / player archetype / hole archetype) with 3-letter abbreviations (`formatArchetypeAbbreviation`), specifically so a new pick never reflows the row — a long name wraps within its own column instead. Now also renders `DraftProgressDots` under the title, and its rows use the same base-grey/gold-legend inset pattern as `HoleResultRow` (see fix #3 above).
- `DraftPage` (`src/pages/DraftPage.tsx`) — **no longer auto-fires the simulation the instant the 18th golfer is drafted** (session history #30). Previously a `useEffect` called `onComplete(state.picks)` (i.e. `finishDraft`) the moment `state.status === 'complete'`, showing only a flash of "Draft complete, simulating…" text. That `useEffect` is gone; instead, on `status === 'complete'` the page renders a "Your bag is set" header, a **"Tee off"** `Button` (the one and only thing that now calls `onComplete`), and the full `DraftRoster` below it so the player can review their finished bag before committing. The existing `useLayoutEffect` that auto-spins each hole's country wheel is untouched — only the post-18th-pick behavior changed.
- `Scorecard` (`src/components/scorecard/Scorecard.tsx`, the per-hole list) is a 2-column CSS grid per row (text column + a mark column spanning both the name and commentary lines) — this was a real fix this session, commentary text used to be able to visually run under the score mark when it wrapped to two lines.
- `ThemeToggle` (`src/components/nav/ThemeToggle.tsx`) — light/dark toggle in the nav bar's top-right, a pill switch with the active `SunIcon`/`MoonIcon` (`ui/icons.tsx`) sliding inside the thumb. Backed by `useTheme` (`src/hooks/useTheme.ts`), see "Design system" below for how it interacts with the existing system-preference CSS.
- `HowToPlayDrawer` (`src/components/nav/HowToPlayDrawer.tsx`) — the slide-out rules drawer panel. **No longer owns its own open/closed state** (see session history #27) — reads `isOpen`/`close` from `useHowToPlay()` so more than one trigger can open the same instance, and is rendered exactly once, from `App.tsx` (not `NavBar` — it's shared chrome now, not nav-specific, even though it's still visually anchored below the nav). `HowToPlayTrigger.tsx` (the nav's 28px circle-bordered "?" button) and the home page's "How it works" `Button` are the two current triggers, both just calling `open()`. Fixed below the nav (`top: 64px`, matching the real nav height, not a token — if the nav's height ever changes this needs updating too), sliding in from the left via a `slideIn` keyframe (skipped under `prefers-reduced-motion`). Closes three ways: the backdrop (a real `<button>`, not a `div` with `onClick`, so it's keyboard-reachable), the header's close button, or Escape (a `document`-level `keydown` listener while open, still owned by the drawer component itself). Also locks `document.body` scroll while open; focus-restore-on-close is generic now (`HowToPlayProvider` captures `document.activeElement` in `open()`, restores it in `close()`) rather than a single hardcoded trigger ref, so it correctly returns focus to whichever of the (now two, possibly more later) triggers was actually used. Reuses the real `ScoreMark` component for the scorecard-notation legend and `ArchetypeBadge`/`ALL_ARCHETYPES` for the archetype legend and inline archetype mentions, rather than hand-rolling new styles for either. **No full focus trap** (Tab can still escape into the page behind the backdrop) — deliberately skipped as out of scope for what was asked; revisit if it's ever raised. Rules copy is real (user wrote it directly into the component) — goal, draft, archetypes, play, scorecard — not placeholder.
- `Footer` (`src/components/ui/Footer.tsx` + `.module.css`, new, session history #30) — rendered once from `App.tsx`, right after the `.page` view wrapper and before `HowToPlayDrawer` (same "shared chrome, rendered once" pattern as `NavBar`/`HowToPlayDrawer`), so it appears at the bottom of every `View`. 10px text at `var(--text-muted)` with `opacity: 0.7` (deliberately more knocked-back than the app's normal muted-text usage — this is boilerplate, not content). Copy is a golf-terminology rewrite of a standard fan-project disclaimer the user supplied (originally football terminology) — "independent fan-made golf draft and round simulator... not affiliated with... any golf tour, championship, club..." etc. Sits naturally after page content on tall pages (e.g. Stats) and pins to the bottom of the viewport on short ones (e.g. Home), for free, because `.page`'s existing `flex: 1` in the `#root` flex column absorbs the remaining space ahead of it — no extra positioning needed.
- `StatsTrigger` (`src/components/nav/StatsTrigger.tsx`, new, Phase A) — the nav's stats entry point, rendered leftmost in `NavBar`'s `.right` cluster (before `HowToPlayTrigger`/`ThemeToggle`). Its own `StatsTrigger.module.css` (a pill, not the circular `.trigger` `HowToPlayTrigger` reuses from `HowToPlayDrawer.module.css`) — deliberately carries a visible "Stats" text label next to the `StatsIcon`, per explicit user ask, since it navigates to a whole page (`viewStats()`) rather than opening an overlay, so it reads as a real nav destination rather than an icon-only utility toggle.
- `StatsPage` (`src/pages/StatsPage.tsx` + `.module.css`, new, Phase A) — the `'stats'` view. Loads `loadStats()` once via `useState(() => loadStats())` (a fresh load every time the page mounts, since `App.tsx`'s view switch fully unmounts/remounts pages — no live-update case to handle). Renders, in order: a **Career** card (`deriveStats`, `StatsGrid` with `large`), a **"By course"** card, a **"Top players"** card (`PlayerLeaderboard`, see below), then **"Round history"** (newest first, course name/date/`formatRelativeScore(totalStrokesToPar)`/a "Bogey-free" badge). Empty state (`rounds.length === 0`) shows a subtitle line plus a "Let's Go" `Button` (calling `playAgain()`, same "reset and go to home" action the nav logo uses) instead of the stats cards.
  - **Career-vs-course visual hierarchy** (session history #30): the user asked for Career to read as the page's clear focal point rather than visually equal to the per-course breakdowns. Two mockup directions were pitched as an Artifact (`https://claude.ai/code/artifact/64168687-8ae8-4960-87a6-55f75f83f40e` — "Scale & Weight": same card shape everywhere, Career just bigger/bolder; vs. "Hero & Compact Roll": Career becomes a `ResultsHero`-style hero, course stats collapse to a compact list) — **the user picked a hybrid**: Career's *card* styling from Approach 1 (`.careerCard`/`.careerGrid`/`.careerStatValue` in `StatsPage.module.css` — bigger padding, a solid 3px accent top edge, a tinted wash background, 32px stat values vs. the normal 22px), combined with the *course breakdown* structure from Approach 2 (no more one full card per course — a single "By course" card containing one compact row per course, name + an inline stat string like "8 rounds · 1 bogey-free · 2 fewest bogeys · 3 birdies · 2 eagles · 1 ace"). `StatsGrid`/`StatItem` both take an optional `large` prop to switch between the two stat-value sizes rather than being two separate components.
  - **Per-course country flags**: `Course` (`content/types.ts`) gained an optional `countryIsoCode` field (`courses.json`: `"US"` for Augusta National, `"GB-SCT"` for Carnoustie), rendered via the existing `CountryFlag` component in each "By course" row — reused directly rather than inventing new flag logic.
  - **Grammar**: a local `pluralize(count, word)` helper handles "1 round" vs. "8 rounds", "1 eagle" vs. "2 eagles", "1 ace" vs. "0 aces". "Fewest bogeys" is a fixed phrase (not run through `pluralize`, same as "Most birdies"/"Most eagles" being fixed category labels elsewhere) — the user specifically asked for "fewest bogeys" wording after an initial "low bogey(s)" attempt read awkwardly.
- `PlayerLeaderboard` (`src/components/stats/PlayerLeaderboard.tsx` + `.module.css`, new) — career golfer leaderboard, added after a user follow-up request (post-Phase-A) specifically asking for "who's been most successful for me," with the ranking logic clarified via `AskUserQuestion` rather than guessed: **ranking basis is total points across every hole a golfer's been drafted for, but only golfers with ≥3 holes played** (`LEADERBOARD_MIN_HOLES_PLAYED` in `deriveStats.ts`) are eligible, so one lucky hole in a single appearance can't outrank a long track record. **Tier point values, given directly by the user**: hole-in-one 8 / eagle 4 / birdie 2 / par 1 / bogey+ −1 (`TIER_POINTS` in `deriveStats.ts`, private to that module) — chosen so a single eagle always outweighs two pars, and a bogey+ actively costs points rather than sitting neutral. Ties break by holes-played (more volume ranks higher), then `golferId` alphabetically as a final stable tiebreak. `rankGolfers()` also returns each golfer's full `tierCounts` breakdown (`Record<OutcomeTier, number>`), which the component renders as a second line under each row — small colour-coded chips (`N× Eagle`, `N× Par`, etc.) reusing the exact same 5-tier colour language as `HoleOutcomeDots` (gold HIO, green eagle/birdie, grey par, red bogey+) — added per an explicit follow-up ask ("show a breakdown of how the points were earned"), not part of the original leaderboard scope. Shows the top 5 by default; a "Show all N players"/"Show top 5" toggle (`useState`, no persistence) reveals the full list. Rows reuse the app's standard neutral-grey/gold-legend inset-bar row pattern (gold for `golfer.skill === 'legend'`) and `CountryFlag`, same as `HoleResultRow`/`DraftRoster`. Empty state (no golfer has reached the threshold yet) shows a one-line hint rather than an empty card.

## Design system

- Colors/tokens in `src/index.css`: `--accent` (indigo `#5b4fe8`), `--surface`, `--shadow`, tier colors (`--tier-gold/green/grey/red`), per-archetype colors. **`--tier-grey` was unused until the dot-indicator design above finally gives it a job (par colour).**
- **Theme**: defaults to the OS's live `prefers-color-scheme` via a plain CSS media query, same as always — `useTheme.ts` only ever sets `document.documentElement.dataset.theme` (`'light' | 'dark'`) once the user actually clicks the nav's `ThemeToggle`, and persists that choice to `localStorage` under the `theme` key from that point on. Until then, no `data-theme` attribute exists at all, so nothing about existing behavior changed. `index.css` has three colour blocks as a result: the base `:root` (light values), `@media (prefers-color-scheme: dark) { :root:not([data-theme='light']) { ... } }` (system dark, unless overridden to light), and `:root[data-theme='dark'] { ... }` (explicit override, same dark values duplicated — unavoidable, a media query can't be OR'ed with an attribute selector in one rule). **If you add a new colour token, add it to all three blocks**, or the explicit-dark-override case will silently fall back to the light value.
- `Button`, `ArchetypeBadge` — shared components, see prior history below for how they got their current shape.
- **Legend-tier indicator**: an inset gold `box-shadow` on whichever container actually reads as "the card/row" to the user (`GolferReels`' `.column`, or a row's own box) — not a separate badge component. On row-based lists (`HoleResultRow`, `DraftRoster`), **every** row carries the same inset bar + left padding in a neutral grey by default, with the legend variant only swapping the colour to gold — never add the bar/padding to the legend row alone, or non-legend rows misalign (see fix #3 above; this bit the first pass at it).
- Course cards (home page) still use no real photography (deferred due to licensing risk, explicit user choice) — but as of session history #27, no longer per-course *varied* gradients either. Every course now gets the identical neutral `ResultsHero`-style accent wash; only a future "major championship" flag (not built) would ever introduce visual variety again, and only for 4 courses, not all of them. Don't reintroduce per-course colour without the user asking — it was explicitly rejected once already.

## Testing

Vitest, 51 tests, all on pure logic (no component/UI tests — UI verified manually via the Browser preview tool every session):
- `src/game/draft/engine.test.ts` — repeat-cap (including the per-country override), bench-exhaustion, no-duplicates, full-18-hole invariants across ~200 seeds.
- `src/game/simulation/engine.test.ts` — odds-config content validation, frequency tests, fixture tests.
- `src/game/simulation/skill.test.ts` / `affinity.test.ts` — skill/fit math invariants.
- `src/game/stats/deriveStats.test.ts` (new, Phase A) — round-level reducers (bogey-free count, lowest/highest tier counts, career tier sums, null-safe empty-input defaults) plus `rankGolfers`: threshold exclusion, the eagle-beats-two-pars weighting, the points→holesPlayed→golferId tiebreak order, a custom-threshold case, and the per-tier `tierCounts` breakdown.
- `src/game/simulation/commentary.test.ts` — no unfilled template placeholders across every tier/fit/par combo.

## Recent session history (roughly chronological; older entries trimmed for space — full detail in git log / prior doc versions if needed)

1–8. MVP build, real content curation, skill tiers, visual identity pass (Sora/indigo/nav/scorecard-as-real-scorecard/confetti), UX pacing, course preview page, live "Your bag" roster, mobile-responsiveness fixes.
9–11. Odds rework to continuous archetype-fit blending; several rounds of "Your bag" iteration ending on the current 3-column grid + icon + abbreviation design.
12. Roster overhaul (19 countries/124 golfers, `repeatCap`, synthetic "Others" country) + real Northern Ireland flag image via `CountryFlag`.
13. **Results table redesign + share removal** (commit `bce934f`): replaced the plain text outcome label with `ScoreMark` (real scorecard notation, shared with the grid above it); added each golfer's flag; gave the mark column a fixed width; fixed commentary text visually running under the mark by making the row a real 2-column grid; added the legend gold-edge to the results list too; **deleted the entire `share/` feature** (image generation + share button) at the user's request, including its whole component directory (nothing else referenced it).
14. **iOS reel-animation bug** — reported as "the country/player spinners don't animate on iOS, they just jump to the result." Took two real attempts: first a forced-synchronous-reflow fix to the CSS-transition trigger (didn't work), then a full rewrite of `Reel.tsx` to use the Web Animations API (`element.animate()`) instead of a hand-triggered CSS transition, which sidesteps a known class of WebKit timing bug rather than patching around it (commit `3f503eb`). **Turned out neither fix was the actual problem** — the user confirmed they had iOS's "Reduce Motion" accessibility setting on, and the code was correctly (and pre-existingly) honoring `prefers-reduced-motion` the whole time. Not a bug. The WAAPI rewrite is still a genuine improvement and stayed in.
15. Course name added to the live reveal screen; the hole-18-skipped-in-reveal bug found and fixed; `?simReveal` debug param added (see above) after the user asked for a faster way to verify reveal/results changes without redrafting 18 holes each time (commit `1057b70`).
16. **Design exploration for the results/reveal experience** — see the pinned section at the top of this doc. Two directions pitched and mocked (as HTML artifacts, no real code), then both expanded to full-site mockups (home/course-info/draft/reveal/results) after the user asked to see them beyond just the results screen. User chose Direction B and gave 3 specific fixes to fold in.
17. **Direction B implemented for reveal/results (stage 1 of the plan)** — New shared components: `RoundHero`, `HoleOutcomeDots`, `CurrentHoleCard`, `HoleResultRow`, plus a `size` prop on `ScoreMark`. `RevealSequence.tsx` and `Scorecard.tsx` rewritten to assemble them; `RevealSequence`'s dead `isComplete` prop dropped in the process (it could never be `true` while mounted — `ResultsPage.tsx` already swaps to `Scorecard` at that point). Fixes #2 (dot sizing) and #3 (legend row padding) from the mockup notes done at this point; fix #1 (draft-screen archetype prominence) deferred, out of scope for this stage. Verified in the browser (light/dark, 320px–400px widths, the live-then-skip-to-final flow).
18. **User review of stage 1 caught two real bugs, both fixed, then Direction B implemented for the draft screen (stage 2)**.
    - Review feedback: (a) non-legend rows had no left inset/padding at all, so their text sat visibly further left than legend rows' — fixed by giving every `HoleResultRow` the same grey inset bar + padding, with `.legend` only recolouring it (see fix #3's "generalized further" note above); (b) the dot strip was a fixed 230px block bunched at the hero card's left edge instead of filling it — fixed with `justify-content: space-between` on the dot container (see dot indicator spec's "post-review refinement" above).
    - Stage 2 (draft): confirmed fix #1 (archetype prominence) needed no work — `HoleHeader.tsx` already had it from an earlier session. Built `DraftProgressDots.tsx` (two-state dot strip, sharing `HoleOutcomeDots.module.css`) and wired it into `DraftRoster.tsx`; applied the same corrected base-grey/gold-legend row pattern to `DraftRoster.module.css`'s rows.
    - Verified by actually drafting several holes in the browser (not just `?simReveal`/`?simResults` mocks) in both light and dark mode, including a legend-tier pick (Vijay Singh) to confirm row alignment. **Items 17–18 landed together as commit `99ce1fb`** ("Redesign reveal, results, and draft screens (Direction B)") — the doc updates were made incrementally but the code was one continuous, uncommitted body of work until this commit.
19. **Asked the user how much of stage 3 (home/course-info) to do** — offered three options (minimal meta-line fix, full mockup parity, or skip). **User chose to skip it entirely**, saying they want to redesign those two pages in detail later rather than build the incremental Direction B version now. Direction B rollout is considered concluded at reveal/results + draft; nothing further planned for home/course-info until the user starts that fresh design conversation. Nothing changed in code this round — doc-only update.
20. **Light/dark theme toggle added to the nav** (commit `9fb21ac`, unrelated to the Direction B work) — a small pill switch, top-right of `NavBar`, sliding a `SunIcon`/`MoonIcon` thumb. `useTheme.ts` only sets `document.documentElement.dataset.theme` once the user actually clicks it (persisted to `localStorage` from then on) — until that first click, the app keeps following the OS's live `prefers-color-scheme`, so nobody who never touches the toggle sees any behavior change. Required adding an explicit `:root[data-theme='dark']` override block to `index.css`, alongside the existing `@media (prefers-color-scheme: dark)` block (now guarded with `:not([data-theme='light'])` so an explicit light choice can still win over a dark OS preference) — see "Design system" above for the "add new tokens to all three blocks" gotcha this introduces. Verified in the browser: toggles both directions, persists across a reload, no console errors.
21. **Course cards on the home page halved in height** (280px → 140px, commit `f33970e`) — they use gradient placeholders rather than real photography, so the taller image-oriented height wasn't needed. Verified in the browser at desktop and mobile widths. Separately, `.claude/launch.json` got `"autoPort": true` (commit `412c3f5`) so this session's dev server didn't hard-fail when another session already held port 5173.
22. **Bogey-free-round odds analyzed and rebalanced** (commit `80f1ce2`) — user asked for the actual probability of a bogey-free round given the real game logic. Computed it directly (holes resolve independently, so it's just the product of each hole's `1 - bogey_plus` chance) across three scenarios, verified against the real `affinity.ts`/`skill.ts`/odds-config code via a throwaway vitest scratch file (written, run, then deleted — not part of the real suite): **no-strategy** (random golfer per hole) ~1.7%/1.4% (Augusta/Carnoustie); **smart drafting** (always take the matching archetype, whatever skill you're offered) ~8.4%/6.8% (≈1-in-12/1-in-15); **legend-ceiling** (every hole perfectly matched to a legend) ~37.6%/34.8% (≈1-in-3).
    - User felt the "smart drafting" number was too low to keep players engaged and wanted ≈1-in-8–9. **`solid` can't be the lever** — `skill.test.ts` explicitly asserts it's a strict no-op at every fit level (it's the neutral baseline the whole matched/unmatched odds table is calibrated around); touching `legend` would move the ceiling number the user already said was fine. That leaves `elite`'s `BOGEY_REDUCTION_RANGE` in `skill.ts` as the one safe lever — and since "smart drafting" is defined at `archetypeFit = 1` exactly, only `elite.max` (not `.min`) affects that number at all.
    - Raised `elite.max` from `0.3` to `0.5` (`skill.ts`). New smart-drafting odds: **Augusta ~10.35% (1-in-9.7), Carnoustie ~8.48% (1-in-11.8)** — legend ceiling unchanged (still 37.6%/34.8%), `solid` untouched. All 39 existing tests still pass unmodified (none hardcode `elite`'s specific reduction value). If the user wants it tuned further, `elite.max = 0.55` gets Augusta to ~1-in-9.2 at the cost of narrowing the elite/legend gap slightly (0.55 vs legend's 0.65 max, down from the current 0.5/0.65 gap) — no urgency to revisit unless raised again.
23. **Results-only hero card redesigned** (commit `d8cb6dd` — `ResultsHero.tsx`/`.module.css`, `Scorecard.tsx` updated) — user wanted the results screen to feel like a "final destination" rather than a copy of the live reveal, specifically calling out that the hero card should weight the bogey streak (the actual goal) at least as much as the score, while everything else on the page (grid, dots, per-hole rows) stays exactly as-is. Went through a real design pass rather than jumping to code:
    - Pitched **two directions as an Artifact mockup** (`https://claude.ai/code/artifact/5f420381-7ced-4768-83aa-d040e47dd110` — reuses the exact Direction B `.evo` tokens/Sora font-face from the earlier full-site mockups, same splice-in-the-base64-via-Bash technique since the font data URI is too large to pass through normal tool-call context): **Direction 1 "Streak Headline"** (streak becomes the card's bold headline, score demoted to a compact stat below a hairline) vs. **Direction 2 "Split Panel"** (streak gets a wider tinted panel, score a narrower neutral one beside it, dots full-width underneath). **User picked Direction 2**, with four refinements: (a) course name promoted to a heavy uppercase eyebrow (borrowed from Direction 1's treatment) at the top of the card; (b) win state washes the *whole card* gold (Direction 1's effect) rather than just the streak panel — panels go transparent so the shared gradient reads through both; (c) win copy changed from "Bogey-free, all 18!" to "Bogey-free for the entire round!", sized down 17px→15px to fit; (d) confirmed the 320px-narrow case wraps gracefully (score panel stretches to match via plain flex row, no fixed heights) and isn't worth over-engineering since 18 holes is a hard ceiling.
    - Iterated the mockup once in place (same Artifact URL, redeployed) with all four refinements before writing any real code — user approved the second pass, then it was built as `ResultsHero.tsx`.
    - `RoundHero` (shared reveal+results before this) is now **reveal-only** — `ResultsHero` is a deliberately separate component with a simpler prop shape (no `revealedCount` slicing logic, since results are always a complete round) and its own win copy. This is an intentional divergence, not an inconsistency to fix later.
    - Verified in the browser: partial state matches the mockup exactly (`get_page_text` confirms "STREAK / Bogey-free through hole 4" and "FINAL SCORE / -9" render as designed); win state verified by temporarily editing `mockSimulationResult.ts` to remove its `bogey_plus` holes (file was clean, reverted via `git checkout` immediately after — confirmed back to original via `git status`/`git diff`), which surfaced "🏆 Bogey-free for the entire round!" and confirmed via computed styles that the `.win` gold gradient actually resolves. Confirmed `RevealSequence`'s hero is untouched (`?simReveal` still shows the old "AUGUSTA NATIONAL · TOTAL" / chip treatment). Dark mode checked too. Build/lint/39 tests all pass.
24. **"How to Play" rules drawer added to the nav** (commit `bbc2224` — `HowToPlayDrawer.tsx`/`.module.css` new, `NavBar.tsx`/`.module.css` and `icons.tsx` updated) — first real UI for explaining the game's rules (previously nowhere in the app). User specced this quite precisely up front: a "?" circle trigger in the nav, a left-sliding drawer fixed just below the nav (nav always stays visible/on top), white background with a subtle shadow, max 400px wide, closable via backdrop click, a close button, or Escape.
    - Mocked first, one Artifact pass (`https://claude.ai/code/artifact/d9cb30c9-3715-40b8-b35e-c9de86e0df9a`, same font-splice technique as the other Direction B–era mockups) — two frames (a 700px "desktop-ish" one showing it as a page overlay, a 375px one showing it fill-width on mobile), both with real first-draft rules copy (goal / draft your bag / play your round / scorecard legend) rather than lorem, since the user said content comes later anyway but empty boxes wouldn't validate sizing/scroll. Flagged four judgment calls in the mockup's own notes rather than blocking on them: "white background" read as `var(--surface)` (adapts in dark mode) not literal `#fff`; added a subtle backdrop scrim rather than an invisible click-catcher; reused the app's existing `--shadow` token as-is rather than inventing a directional one; put the trigger left of the theme toggle. **User approved as-is, all four calls included, no changes requested.**
    - Built exactly to the approved mockup. `ScoreMark` (the real component, not new mark styles) reused for the scorecard-notation legend in the drawer. Two additions beyond the mockup's own scope, since a static mockup can't demonstrate them: a `document.body` scroll lock while open, and focus management (close button gets focus on open, focus returns to the trigger on any close path). Deliberately did **not** add a full focus trap (Tab can still leave the drawer into the page behind it) — wasn't asked for, and easy to get subtly wrong if added speculatively.
    - Verified all three close paths (backdrop click, close button, Escape) plus the open trigger, in the browser, in both themes, at desktop and mobile widths — one false alarm along the way where the close button appeared not to work, which turned out to be Vite HMR staleness after several hot-reloads mid-session, not a real bug (confirmed by a hard reload, after which all three close paths worked first try). No console errors. Build/lint/39 tests all pass.
25. **User fleshed out the drawer's rules copy directly** (editing `HowToPlayDrawer.tsx` themselves, outside this conversation — a mid-session file-change note flagged it) — added real sections (goal, draft your bag, archetypes, play your round, scorecard) with three bracketed placeholders (`[LONG HITTER]`, `[PRECISION IRON]`, `[LEGENDS]`) left for styling. Asked to: (1) add a list of all 5 archetypes under the "Archetypes" heading, uppercase, in their branded colours; (2)–(3) replace the two bracketed archetype placeholders with real archetype-styled text inline in a sentence; (4) replace `[LEGENDS]` with "Legends" (title case, not uppercase — deliberately different from the archetype badges) in `--tier-gold`.
    - Reused the real `ArchetypeBadge` component for all of this rather than hand-rolling new styles — it's already exactly "uppercase, bold, archetype-branded colour" (see "Design system" below), and `ArchetypeBadge` is `display: inline-block` so it drops into running sentence text cleanly for the inline replacements. Added `ALL_ARCHETYPES` (`Object.keys(ARCHETYPE_LABELS) as ArchetypeTag[]`) to `formatArchetype.ts` as the canonical list of all 5 tags, rather than hardcoding the array a second time in the drawer component — single source of truth if anything else ever needs "every archetype."
    - "Legends" got a small one-off `.legendText` class (`font-weight: 700; color: var(--tier-gold)`) in `HowToPlayDrawer.module.css` — there's no shared "legend text" component elsewhere in the app (the existing legend-tier treatment is always an inset gold box-shadow edge on a row/card, never styled text), so this is a new, narrow addition rather than reuse of something existing.
    - Left the user's own pre-existing typo ("aoiding a bogey") untouched at first — out of scope for the specific 4 asks, not silently corrected — then fixed it once the user explicitly asked ("aoiding" → "avoiding"). Both the archetype-styling work and the typo fix landed in the same commit (`bbc2224`), along with a couple of the user's own direct copy edits to the drawer (moved the archetype list's position relative to its intro paragraph, switched `.archetypeList` to `flex-direction: column`, bolded "Par is never guaranteed" with `<b>`) — all preserved as-is, not reverted.
    - Verified in the browser (both themes): all 5 archetype badges render in their correct branded colours in the new list, both inline replacements render correctly mid-sentence, "Legends" renders gold and title-case (not uppercase, per the explicit ask), typo confirmed fixed via `get_page_text`. No console errors. Build/lint/39 tests all pass.
26. **`GolferCard` surname wrapping fixed** (commit `4498fa4`) — user flagged two real bugs from screenshots: two-word surnames ("Tom Morris") weren't centered relative to each other when wrapped (`.surname` had no `text-align`), and a long single-word surname ("Ballesteros") was splitting mid-word into an orphan letter on its own line (`word-break: break-word` forcing a break with nowhere sensible to break).
    - Fixed centering directly. For the clipping problem, removed `word-break: break-word`/set `overflow-wrap: normal` so text never force-breaks mid-word — multi-word/hyphenated surnames still wrap fine at their natural space/hyphen, only a genuinely long *single* word needs help. Added `hasLongWord()` in `GolferCard.tsx` (any space/hyphen-delimited segment ≥10 chars) that applies a `.surnameLong` modifier class.
    - **Didn't guess the font-size** — used `javascript_tool` to inject real `GolferCard` DOM (cloned structure, real CSS-module class names pulled live from the page) at the component's actual rendered width (~90–92px, capped by `GolferReels`' `max-width: 420px` on the whole 3-column reel — this ceiling holds on *any* viewport width, not just mobile) and iterated the font-size against the real roster's longest single-word surnames (Ballesteros, Montgomerie, DeChambeau, Bjerregaard, Harrington, Oosthuizen) until none overflowed. 11.5px still clipped "Montgomerie"/"DeChambeau" (wide-glyph names, character count alone doesn't predict pixel width) — landed on 10px + `letter-spacing: -0.01em`. Cross-checked by computing every real golfer's actual surname/first-name split via a throwaway Python script against `countries.json`, not assumption.
    - Mid-turn, user asked to also shrink `firstName` so it's never bigger than the (now smaller) `surnameLong` text, then said to ignore that request one message later — correctly not implemented, `firstName` stayed untouched at its original 12px throughout.
    - Verified against the real compiled CSS (not just the injected mid-test copy) after the file edit, at the real card width taken from a live `GolferCard` in the running app. Build/lint/39 tests all pass.
27. **Home page redesigned** (commit `61136cc`) — user asked to redesign the home page, explicitly scoped small: add a "How it works" CTA opening the existing `HowToPlayDrawer`, and see how the course-card grid holds up with 4 more courses (6 total). This is the "later, fresh design conversation" item 15/16 predicted for home/course-info back when Direction B skipped it — and it genuinely was fresh, not a Direction B continuation.
    - **Mockup pass 1** (`https://claude.ai/code/artifact/091559d6-d282-4416-8b58-c943c57c37db`) — kept the existing saturated-gradient course-card style but gave each of the 4 new courses (St Andrews, Pebble Beach, Royal Melbourne, TPC Sawgrass — real courses, picked for gradient variety, not final content) its own gradient grounded in real terrain. **User rejected this direction outright**: no more per-course colour variety at all — planning a future "season" feature where only the 4 majors get a distinct look (like Augusta's current green), which only makes sense if every course is genuinely neutral by default first. Also wanted the cards to align with `RoundHero`/`ResultsHero`'s visual language instead (light accent-tinted wash, not a saturated photo-style gradient), and asked whether the Sora font had been dropped.
    - **Font question had a real answer, not a false alarm**: `.course-card` is a `<button>`, and browsers don't inherit `font-family` into buttons by default — the mockup set Sora on `body`/`.evo` but never reset it on the button elements, so course-card text was silently falling back to the OS system font. Confirmed the cause, added a blanket `button { font-family: inherit; }`, verified fixed — this bug got carried into the real implementation's checklist too (see below).
    - **Mockup pass 2** (same URL, redeployed) — every course card restyled identically: `linear-gradient(160deg, color-mix(in srgb, var(--accent) 12%, var(--surface)), var(--surface))` background, 1px `var(--border)`, 14px radius (exactly `ResultsHero`'s recipe), dark text, "Par N" as an accent-tinted chip instead of a translucent white pill. Added one extra, clearly-labeled preview (not part of the real grid) showing a reserved `.major` variant — gold edge + gold badge, same idea as the app's existing legend-tier treatment — to prove the future direction would look right, without building any of the data model for it. **User approved as-is.**
    - **Real implementation**: `CourseCard.tsx` lost its `courseId` prop and the whole `COURSE_GRADIENT`/`DEFAULT_GRADIENT` lookup — both were only ever used for the removed gradient, so kept as dead weight would have violated the project's own no-dead-code discipline. No `isMajor` field or `.major` styling was added to real code — the mockup preview was explicitly future-only, nothing to build yet.
    - **The CTA needed real architecture, not just a button.** `HowToPlayDrawer` previously owned its open/closed state internally (self-contained, same pattern as `ThemeToggle`) with only the nav able to reach it. Since the home page CTA needed to open the *same* drawer instance, extracted the state into `HowToPlayContext`/`HowToPlayProvider`/`useHowToPlay` (new, `src/state/`, mirroring `GameContext`'s 3-file split for consistency) and split the old component into `HowToPlayTrigger.tsx` (the nav's "?" icon) and a slimmed-down `HowToPlayDrawer.tsx` (just the panel, rendered exactly once from `App.tsx`, reading `isOpen`/`close` from context). Focus-restore-on-close changed from a hardcoded `triggerRef` to capturing `document.activeElement` in `open()` and restoring it in `close()` — works correctly regardless of which of the two triggers was actually used, not just the nav one.
    - The "How it works" button itself reuses the real `Button` component (`variant="secondary"`) rather than inventing new button CSS, with a small circle "?" icon (`border: 1.5px solid currentColor` so it always matches the button's own text colour with no separate token to keep in sync).
    - Verified in the browser: both entry points (nav icon, home CTA) open the same drawer; Escape closes and returns focus to *whichever* button was actually clicked (confirmed by screenshot — focus ring visibly on the right button both times); course selection still navigates correctly after `CourseCard`'s prop simplification; dark mode checked. One red herring along the way — `read_console_messages` on a long-lived, many-times-hot-reloaded tab showed a stale `useHowToPlay must be used within a HowToPlayProvider` error from mid-refactor HMR cascading; confirmed clean (zero errors) on a genuinely fresh tab, closed the stale one. Build/lint/39 tests all pass.
28. **Roadmap planning session for stats/accounts/season** (plan-mode, no code touched) — see the pinned section at the top of this doc for the full summary, and `/Users/ash.robbins/.claude/plans/ok-so-for-an-crystalline-ladybug.md` for the actual plan. Notable process detail for a future session: an Explore agent found the project's own root-level `golf-draft-game-plan.md` already had a "Tour Season" concept sketched out (16 rounds: 12 standard + 4 majors) — that's what caught the user's stated "12 courses" not actually matching their own stated 3-regular-plus-1-major-times-4 pattern (which is 16), a real discrepancy that got resolved by asking rather than silently picking one. Supabase pricing was verified directly against `supabase.com/pricing` (not just aggregator blog posts) via `WebFetch`. Course shortlist for the season went through two rounds of user-requested swaps after the first proposal (Southern Hills → Pinehurst No. 2 for the 4th major; Sun City → Kasumigaseki → Jumeirah Golf Estates for the last regular slot, landing on Jumeirah after the user rejected Kasumigaseki as "a bit obscure"). Once the plan was approved, created the `stats` branch off `main` (at `0001ad4`) for this work — nothing implemented yet.
29. **Phase A built: local stats tracking, plus a career player leaderboard added on top per a same-session follow-up ask.** (Landed in commit `a91e491` along with session history #30's work below — see that entry.) Two sub-parts:
    - **Phase A itself** (the plan's spec, built essentially as scoped): new `src/game/stats/` module (`types.ts`/`storage.ts`/`deriveStats.ts`), `recordRound()` wired into `GameProvider.tsx`'s `finishDraft()`, a new `'stats'` `View` + `viewStats()` action, `StatsPage.tsx` (career + per-course `StatsGrid` cards, round history), and a nav entry point (`StatsTrigger.tsx`, initially icon-only reusing `HowToPlayDrawer.module.css`'s `.trigger`). `deriveStats.test.ts` added (7 tests at this point). Verified end-to-end in the browser, not just unit tests: played a full real 18-hole draft on Augusta (not `?simResults`, to actually exercise `finishDraft()`) via the Browser tool's `read_page`/ref-based clicking (country-pill click → "Select" ref → "Draft <name>" ref, repeated 18×, since coordinate-based clicking proved unreliable against the reel's render timing), confirmed the resulting `RoundRecord` in `localStorage` matched the real drafted golfers hole-by-hole, confirmed the Stats page displayed correct career/per-course aggregates, confirmed persistence across a reload, and confirmed `?simResults` does **not** add a round (localStorage round count unchanged after visiting it) — the debug shortcuts bypass `finishDraft()` entirely, by design.
    - **Player leaderboard follow-up** (user asked mid-session, before Phase A was committed, for a "top 5 most successful players" table above round history) — ranking logic was **not** guessed: two rounds of `AskUserQuestion` nailed down (a) ranking basis — total points, golfers need ≥3 holes played to qualify (`LEADERBOARD_MIN_HOLES_PLAYED`) — and (b) exact tier point values, which the user gave directly after rejecting two suggested presets: **hole-in-one 8 / eagle 4 / birdie 2 / par 1 / bogey+ −1**. Built as `rankGolfers()` in `deriveStats.ts` (returns points, holes played, and a full `tierCounts` breakdown per golfer) plus a new `PlayerLeaderboard` component/CSS module under a new `src/components/stats/` directory, inserted into `StatsPage` between the per-course cards and round history as asked. Then, a second mid-session follow-up asked for (1) the word "Stats" added to the nav trigger and (2) a visible breakdown of how each golfer's points were earned. For (1), `StatsTrigger` was rebuilt with its own `StatsTrigger.module.css` (a labeled pill, not the icon-only circle it started with) — the now-unused icon-sizing class this displaced (`.triggerIcon`, added to `HowToPlayDrawer.module.css` minutes earlier for the icon-only version) was removed rather than left dead. For (2), each leaderboard row grew a second line of small colour-coded tier-count chips, reusing `HoleOutcomeDots`' exact 5-tier colour mapping. 5 more tests added to `deriveStats.test.ts` (12 total in that file, 51 project-wide). Verified in the browser by seeding realistic `localStorage` data directly (real golfer/country ids from `countries.json`, including several legend-tier golfers and one golfer deliberately left under the 3-hole threshold) rather than playing multiple full 18-hole drafts — a deliberate efficiency call once the write path was already proven in the Phase A verification above; confirmed the ranking order, tiebreaks, threshold exclusion, tier-breakdown chips (colours and counts), and the show-all/show-top-5 toggle all matched hand-computed expectations, in both light and dark mode.
30. **Career/course visual hierarchy, per-course flags, site footer, and two new CTAs — then everything committed.** Four separate user asks, handled in sequence:
    - **Career-vs-course hierarchy.** User asked for Career to read as the stats page's clear focal point, with per-course blocks "slightly smaller and less visually equal," and explicitly asked for two mockup approaches first. Pitched as an Artifact (`https://claude.ai/code/artifact/64168687-8ae8-4960-87a6-55f75f83f40e`, using the app's real tokens/Sora-via-Google-Fonts rather than a from-scratch visual identity, since this is a UI hierarchy tweak to an existing product, not a fresh editorial design): **Approach 1 "Scale & Weight"** (same card shape everywhere, Career just bigger/tinted, course cards shrink into a 2-up row) vs. **Approach 2 "Hero & Compact Roll"** (Career becomes a real hero card, course stats collapse into a single compact list). **User picked a hybrid mid-message, before finishing review of either mockup in isolation**: Career's card treatment from Approach 1, course breakdown's list structure from Approach 2. Built exactly that combination directly into `StatsPage.tsx`/`.module.css` (see the `StatsPage` component note above for the CSS specifics) — verified in the browser with seeded multi-course `localStorage` data, light + dark, desktop + mobile (375px, where the compact stat string wraps under the course name — acceptable, not worth more engineering for a 2-course app).
    - **Per-course flags.** User shared a screenshot of the desired "By course" row shape (flag + name + stat string, with correct singular/plural nouns like "1 ace" vs. "0 aces"). This needed real data the app didn't have yet — `Course` had no country field. Added `countryIsoCode?: string` to the `Course` type and to both real courses in `courses.json` (`"US"`, `"GB-SCT"`, matching the exact ISO codes `countries.json` already uses for USA/Scotland so `CountryFlag` renders identically to golfer flags elsewhere), reusing `CountryFlag` rather than inventing new flag logic. Also fixed pluralization on rounds/birdies/eagles/aces (previously always-plural) via a small local `pluralize()` helper. **Follow-up correction**: user asked for "low bogeys" (which had been pluralizing to "1 low bogey"/"2 low bogeys") to instead read as the fixed phrase **"fewest bogeys"** — changed to not run through `pluralize` at all, matching how "Most birdies"/"Most eagles" are fixed category labels elsewhere in the app, not dynamically pluralized nouns.
    - **Site footer.** User supplied real football-site disclaimer copy and asked for a golf-terminology equivalent, added to the bottom of every page at 10px in a knocked-back colour. Rewrote the copy directly (independent fan-made project, no official branding/likenesses, informational/editorial use only, trademarks belong to their owners) and built `Footer.tsx`/`.module.css` (new, `src/components/ui/`), rendered once from `App.tsx` — see the `Footer` component note above for how it naturally pins to the bottom of short pages via the existing `#root` flex-column layout.
    - **Two new CTAs.** (a) Stats page empty state (no rounds played) gained a subtitle + **"Let's Go"** `Button` wired to `playAgain()` — reusing the existing "reset state, go home" action rather than inventing a new one, since that's exactly what "go start a round" means from an empty stats page. (b) `DraftPage`'s auto-simulate-on-18th-pick behavior was replaced with a **"Tee off"** `Button` the user must click — see the `DraftPage` component note above for the before/after. Verified end-to-end in the browser: played a full genuine 18-hole draft (async polling loop via `javascript_tool`, clicking "Select" then "Draft <name>" each hole — **note**: this stalled partway through once, because the browser tab wasn't foregrounded and Web-Animations-API-driven reel timing appears to be throttled/paused in backgrounded tabs; foregrounding the tab via `tabs_select` before resuming fixed it, worth remembering for any future scripted draft-through), confirmed the draft stops at "Your bag is set" instead of auto-advancing, confirmed "Tee off" correctly kicks off the reveal sequence, and confirmed the empty-stats "Let's Go" button correctly navigates home. No console errors either theme.
    - **Committed.** All of items #29–30 landed together in a single commit, `a91e491` ("Add player stats tracking, career leaderboard, site footer, and draft/stats CTAs"), on the `stats` branch — `PROJECT_CONTEXT.md` deliberately excluded from that commit and updated/committed separately afterward, matching this repo's established doc-then-code-commit-then-doc-commit pattern. Lint/build/51 tests all pass at the point of commit.
31. **`stats` merged to `main` via PR #1** (merge commit `ced2a14`, done outside this session — the user pushed the branch, opened, and merged the PR on GitHub) — a session then picked up on `main` directly rather than `stats`. First ask on `main`: hide the nav's "Stats" text label below 560px (matching the breakpoint `CourseHoleTable` already uses) so the site title has room on narrow screens, leaving just the icon — `StatsTrigger` grew a `.label` span and a small media query, plus `aria-label="Stats"` on the button so the accessible name survives the label's disappearance. Commit `056e0cc`.
32. **Three UI polish asks, one commit (`c99feef`).** (a) A second reveal-sequence "Skip" button added under the current hole's commentary card (not just the one after "Holes so far"), so it's reachable without scrolling past a growing list. (b) Round history's bogey-free rows switched from a text badge that misaligned date/score when the course name wrapped, to the app's standard gold inset-bar row treatment (same convention as `HoleResultRow`/`DraftRoster`) with the badge moved under the course name and the row itself switched from flex to a `1fr auto auto` grid so date/score never move regardless of name wrapping. (c) "By course" stat strings (a wrapping inline sentence) replaced with a proper 3-column grid per course (`CourseStatsGrid`/`CourseStatItem`), mirroring the Career card's `StatsGrid` pattern at a more compact size.
33. **Local-storage disclosure + a real mobile bug fix (`846ff32`).** Added a one-line, footer-styled (10px, knocked-back `--text-muted`) disclosure to the bottom of the Stats page — shown only once at least one round exists — explaining stats are local-only and won't survive another device/browser, cleared browsing data, or an incognito window. Separately, user reported empty space appearing at the bottom of pages on mobile after scrolling; root-caused to `#root`'s `min-height: 100svh` — `svh` is pinned to the viewport with the mobile browser's address bar *fully expanded* and never grows back once the bar auto-hides on scroll (the real, larger viewport), leaving a gap below the footer. Fixed by switching to `100dvh`, which tracks the actual live viewport. Couldn't fully reproduce the collapsing-address-bar behavior in the Browser tool (no real dynamic toolbar to test against) but the `svh`/`dvh` mismatch is a textbook, well-documented match for the reported symptom.
34. **Logo exploration (Artifact only, no code) then a real draft-screen layout-shift bug fix (code, commit `abe503f`).**
    - User asked for 5 golf-themed logo concepts for "Beating Bogey" (one of their own ideas: reuse the real `ScoreMark` bogey-square notation with a stylised "B"/"BB" inside), plus a wordmark, inspired by several Dribbble-style reference images. Built as an HTML/SVG Artifact (`https://claude.ai/code/artifact/f14db4d0-de40-4f7c-a0ca-b225b729ab0b` — scratchpad file since deleted with the session's temp dir, the published Artifact itself persists) with 5 concepts, each shown at real favicon size (96/32/16px) and locked up with a wordmark in a mock nav bar, light + dark. The strongest was Concept 1 ("The Bogey Mark") — literally the app's own invented notation, most ownable of the set.
    - Follow-up ask: more playful, script-y ways to combine the *two* B's in "Beating Bogey" into one mark. Added 4 more concepts (interlocking mirrored-B monogram seal, a linked cursive "bb", a rotated/faded "echo" B, and two B's joined by a golf-swing arc with a ball). **First pass at the mirrored monogram was genuinely broken** — hand-drawn bowls as solid filled blobs with no counters/holes, so it read as an abstract dumbbell shape, not "BB." Caught on visual review and rebuilt using real mirrored Sora glyphs (`<text>` + `transform: translate() scale(-1,1)`) instead of hand-authored paths — a good example of why this project's mockups always get screenshotted and honestly assessed, not just shipped on the first render.
    - Third follow-up: the user sketched their own rough pen-and-paper mark (squircle frame → circle → a looping glyph) and asked for it vectorized, "inspired by" a green two-tone coin-style app icon reference (dark tile, lighter same-hue circle, monoline glyph). Work was **interrupted mid-flight** by the layout-shift bug report below before finishing — a `bogey-pin-mark.html` exploration exists in a since-cleared scratchpad dir with 3 draft versions (Solid Coin / Outline Frame / Coin-No-Ring), glyph reading as a bold "P" (single loop + stem) rather than confirmed "flag" — **this thread is unresolved, pick it back up if asked**, don't assume it's done.
    - **The actual bug fix**: user shared a screen recording of the draft screen "jumping" during every hole's country/golfer roll. Root cause, confirmed by reading `DraftPage.tsx`/`GolferReels.tsx`: three separate conditionally-mounted blocks each added height only once their content existed — `GolferReels` itself (mounted only after the country resolved), each card's "Select" button (mounted only once its reel settled), and the outer "Draft `<name>`" button (mounted only once a golfer was selected) — each one a fresh layout shift pushing "Your bag" down. Fixed by (a) adding `GolferReelsSkeleton` (new, shares `GolferReels.module.css`'s exact classes so its footprint is pixel-identical) shown while the country's still spinning, and (b) making the Select/Draft buttons always-mounted with a `.pending { visibility: hidden }` class instead of conditional rendering, so their space is reserved from first render. Verified by polling `DraftRoster`'s `getBoundingClientRect().top` every 120–200ms through two full hole cycles (country spin → cards settle → select → draft → next hole) via `javascript_tool` — pinned at exactly 611px the entire time, zero movement. Same commit also gated the reveal sequence's second Skip button (item #32a) to only appear once "Holes so far" has ≥3 rows, since at 1-2 it just duplicated the one above it.
35. **PWA support added** (commit `24064c0`, doc catch-up in `0b84c57`). Installed `vite-plugin-pwa` (v1.3.0, confirmed Vite 8–compatible) and configured it in `vite.config.ts`: `manifest` (name "Beating Bogey", short_name "Bogey", `theme_color` = `--accent` `#5b4fe8`, `background_color` = `--bg` `#f5f5f7`, `display: standalone`), plus a widened Workbox `globPatterns` (added `json`) so the game's static content (`countries`/`courses`/`odds-config.json`) gets precached too — since there's no backend, this makes the installed app work fully offline, not just be installable. **App icon is the real nav-bar ⛳ emoji**, per explicit ask — generated by rendering `'⛳'` onto an HTML canvas at a controlled size (`"Apple Color Emoji"` font stack) via `javascript_tool` in the Browser pane, extracting the `canvas.toDataURL()` base64 PNG, and decoding it to disk with a Python/Bash one-liner (the direct `computer` screenshot path doesn't support pixel-exact cropping, canvas export does). Produced `public/icons/icon-{192,512}.png`, a separately-composed `icon-maskable-512.png` (smaller glyph, more padding, so it survives Android's safe-zone mask crop), and `public/apple-touch-icon.png`/`favicon-{16,32}.png`; deleted the old mismatched purple-splash `favicon.svg`. `index.html` got the icon `<link>`s plus `apple-mobile-web-app-capable`/`-status-bar-style`/`-title` meta tags (iOS ignores the manifest's `display: standalone` without these). **Verified**: production build generates `dist/sw.js` + `manifest.webmanifest` correctly, manifest and all 3 icon URLs fetch 200 over `vite preview`, dev server (`npm run dev`) unaffected (Workbox only activates in a real build — `devOptions.enabled` left at its default `false`). **Not fully verified**: actual service-worker *registration* — it failed with a generic "unknown error fetching script" in the Browser pane tool specifically, despite `sw.js` serving correctly via plain `curl`/`fetch`; most likely the tool's sandboxed/embedded browsing context restricts SW registration (a known category of restriction for embedded/iframed pages) rather than a real config bug, but this couldn't be fully confirmed from inside the tool — **worth a real-device check** (visit the deployed/dev URL on an actual phone and confirm the install prompt / "Add to Home Screen" produces a standalone app with the flag icon) before considering this fully done.
36. **Two new courses (Valderrama, TPC Sawgrass) + a "closer" rationale explainer + home-card flags** (commit `34f45b3`).
    - **Content added the way Augusta/Carnoustie were** — real hole-by-hole par/yardage, not invented, sourced via `WebSearch`/`WebFetch` rather than trusting memory (golf course yardages/pars vary enough across published sources that this mattered): Valderrama initially looked like it might be par 72 in one source, cross-checked against two independent sources to confirm the real par is **71** (6,475 yds, hole 4 signature hole is "La Cascada") before writing anything; TPC Sawgrass matched cleanly on the first source (par 72, 7,245 yds, hole 17 = the famous Island Green). Archetypes assigned by extending the existing convention (see session history #37's explainer below) rather than guessing fresh per course. Both courses got `countryIsoCode` (`ES`/`US`) and were inserted into `courses.json` in the exact order asked — Augusta, Carnoustie, Valderrama, TPC Sawgrass — since the home page's course grid just maps over that array with no separate sort, array order *is* display order.
    - **User then asked, unprompted, for the course's national flag on home-page cards too** (mid-turn, while the course-adding work was still in progress) — added a `countryIsoCode` prop to `CourseCard.tsx`, rendered via the existing `CountryFlag` component, absolutely positioned top-right of the card.
    - **Follow-up: explain the "closer" archetype rule.** User asked why the last 3 holes of every course get `closer` regardless of actual par (a pattern already established on Augusta/Carnoustie, just carried forward onto the two new courses without re-explaining it). Answered without changing anything: every other archetype describes an objective shot-shape demand the hole itself makes (long_hitter=par 5, short_game_specialist=par 3, precision_iron/scrambler=the remaining par 4s by character) — `closer` is different, it's the "elevates under pressure late in a round" trope, which only means something in the context of the round, not a specific hole's par. Fixing it to the final 3 holes gives it that context, guarantees it's actually draftable-and-relevant on every course regardless of that course's own par-3/4/5 mix, and matches how real courses' closing stretches (Amen Corner, Carnoustie's Barry Burn/Island/Home, Sawgrass's 16-17-18) are genuinely the dramatic part of a round.
    - **Same follow-up, a real content change**: user asked to drop hole names entirely from the two new courses ("La Cascada", "Island Green") since neither course has real official names for *every* hole, unlike Augusta/Carnoustie — partial naming read as inconsistent. Both `name` fields removed; `CourseHoleTable`/`HoleHeader` already treat `hole.name` as optional so no component changes were needed, just the content.
37. **Course flag threaded through every screen that shows the course name** (commit `9863f33`) — user asked for the flag next to the course name on the course-preview, draft, and reveal pages, then added the results page mid-turn too. All four now take/pass `countryIsoCode` down to a `CountryFlag`: `CoursePreviewPage.tsx`'s `<h1>`, `HoleHeader.tsx`'s per-hole title (draft screen), `RoundHero.tsx`'s eyebrow label (live reveal, via `RevealSequence`→`ResultsPage`), and `ResultsHero.tsx`'s eyebrow (final results, via `Scorecard`) — all reusing the existing `CountryFlag` component, no new flag-rendering logic anywhere. Verified in the browser across all four screens, light and dark.
38. **Bogey-free odds re-tuned a second time, after the user edited player skills/archetypes in `countries.json` directly (outside any session) and asked for a fresh probability analysis across all 4 courses.** Not a code task at first — a pure-JS scratchpad port of the real `affinity.ts`/`skill.ts`/`engine.ts` logic (reading the actual `countries.json`/`courses.json`/`odds-config.json` content, not fixtures) ran 200,000-round Monte Carlo simulations per course under three strategies: **pure random pick** (any golfer, any hole, no repeats), **archetype match every hole** (deliberately draft the best-fitting available golfer per hole), and **normal gameplay** (the real draft-wheel mechanic — 3 golfers offered per spin from one random country, repeat-cap/bench rules applied — with a random pick among the 3 offered, as a proxy for a non-optimizing player). Initial results: archetype-match ~9.8% (1-in-10) averaged across the 4 courses, normal gameplay ~1.8% (1-in-55) — this matches session history #22's prior "smart drafting ~1-in-9 to 1-in-12" tuning, since nothing in the odds model had changed, only the golfer roster.
    - **User felt both were too punishing** — explicitly wanted archetype-match at ~1-in-5 and, after a first round of tuning landed archetype-match right but left normal gameplay only at ~1-in-30, asked specifically for normal gameplay closer to ~1-in-15. Tuning `skill.ts`'s `BOGEY_REDUCTION_RANGE` alone (raising `legend`/`elite` maxes, easing `journeyman`'s penalty) couldn't hit both simultaneously — pushing skill far enough to lift normal gameplay to 1-in-15 always overshot archetype-match well past 1-in-5, because roughly half the real roster (35 journeyman + 24 solid of 124 golfers) gets little or no skill-shift benefit, capping how far "normal gameplay" (average fit, average skill) can rise without also inflating "archetype match" (near-perfect fit) past target.
    - **Real lever that decoupled the two**: softened `odds-config.json`'s **`unmatched`** distributions (moved probability mass from `bogey_plus` into `par` for all 3 par types — par-3 bogey_plus 0.295→0.20, par-4 0.325→0.22, par-5 0.23→0.15; `matched` tables untouched). Archetype-match play barely touches the unmatched table (it's always drafting near-perfect fits), so this mostly lifts random/normal-gameplay outcomes without moving the archetype-match number much — the two constraints stopped fighting each other once base-odds and skill-shift were tuned as separate levers instead of one.
    - **Final constants** (`skill.ts`): `legend { min: 0.32, max: 0.9 }` (was `0.25/0.65`), `elite { min: 0.1, max: 0.85 }` (was `0.08/0.5`), `journeyman { min: -0.12, max: -0.12 }` (was `-0.18` flat), `solid` left untouched at `{0, 0}` (still the no-op neutral baseline — never touched across either tuning pass, since `skill.test.ts` asserts it directly). **Result, verified via a 200,000-round-per-course/strategy re-run against the real (edited) game files, all 51 existing tests still passing unmodified**: archetype-match averages **18.27% (1-in-5.5)** across the 4 courses (range 16.8%–19.9%), normal gameplay averages **6.81% (1-in-14.7)** (range 6.2%–7.2%), pure random averages 7.47% (1-in-13.4). All within a hair of the user's stated 1-in-5 / 1-in-15 targets.
    - The Monte Carlo scratchpad scripts (a plain-JS port, not real code) lived only in the session's scratchpad dir, not the repo — same "throwaway analysis script, not part of the real suite" pattern as session history #22.
    - **Not yet committed** — `public/content/countries.json` (the user's own direct skill/archetype edits, made outside this session), `public/content/odds-config.json`, and `src/game/simulation/skill.ts` are all modified in the working tree as of this doc update; nothing has been committed or pushed this session.

## Things a future session should know / watch for

- **Country capacity invariant**: `assertWheelHasCapacity()` throws loudly at content-load time if total draft capacity (Σ `min(repeatCap ?? 3, bench size)`) can't fill 18 holes.
- **Per-country repeat cap**: `Country.repeatCap` threaded through as `DraftState.countryRepeatCaps`. The wheel-spin itself stays uniform-random regardless — raising a cap only changes how long a country stays eligible, not its odds of being spun.
- **Reduced motion**: respected by both the spinners and the reveal sequence (see #14 above — verify this is still true if the reveal screen gets substantially rewritten as part of the Direction B work, since the row-by-row table's entrance animation needs the same treatment the mockup already gave it).
- **No dead code policy has been followed carefully** — `share/` was fully deleted rather than left unused; extend the same discipline to whatever the reveal/results rewrite replaces.
- Northern Ireland's flag: see `CountryFlag` above — don't duplicate the special-case logic elsewhere.
- `GolferCard` has exactly one call site (`GolferReels`) — re-check that assumption before adding a second one.
- If asked to source real course photography: explicitly deferred due to licensing risk, not forgotten.
- **`stats` branch is dead** — merged into `main` via PR #1 (`ced2a14`); everything since lives on `main` directly. Don't branch from or compare against `stats`, it's stale.
- **Unresolved thread**: the user's own hand-sketched logo (squircle → circle → looping glyph, "inspired by" a green two-tone coin-icon reference) was being vectorized when interrupted by the draft layout-shift bug report (session history #34) — 3 rough versions exist only in a since-cleared scratchpad file, never published as an Artifact, glyph shape unconfirmed (reads as a "P" to Claude, not confirmed against user intent). If logo work comes up again, this thread was left mid-conversation, not abandoned by choice.
- **PWA service-worker registration** (session history #35) couldn't be confirmed working from inside the Browser tool (registration fails there with a generic fetch error despite `sw.js` serving fine over plain HTTP) — most likely a tool sandboxing limitation, not a real bug, but genuinely unverified. If PWA installability is ever in question, check on a real device before assuming the SW setup is broken.
- **Adding a 5th course**: the closing 3 holes (whatever their real par) always get `archetype: "closer"`, overriding what their par would otherwise map to — see session history #37 for the full rationale (short version: `closer` is a "clutch/pressure" trope, not a shot-shape skill, so it only makes sense tied to a round's final stretch, not a specific par). Keep this rule for any future course rather than reintroducing it as a fresh design question each time. Also: only add a `Hole.name` if the real course has official names for *every* hole (Augusta/Carnoustie do; Valderrama/Sawgrass deliberately don't) — partial naming was explicitly rejected as worse than none.
- **Course flags are now the norm, not an exception** — `Course.countryIsoCode` renders via `CountryFlag` on the home cards, the course-preview title, the draft screen's per-hole header, the live-reveal hero, and the results hero (session history #36–37). Any new course needs a real `countryIsoCode` set, or it'll just silently show no flag everywhere (all these call sites already guard on it being present).
- **Bogey-free odds have now been tuned twice** (session history #22, then #38) — both times by raising `skill.ts`'s `BOGEY_REDUCTION_RANGE` for `legend`/`elite`/`journeyman` (never `solid`, which must stay the neutral `{0,0}` no-op per `skill.test.ts`). The second pass also had to soften `odds-config.json`'s `unmatched` `bogey_plus` rates, because skill-shift alone couldn't lift "normal gameplay" odds without overshooting "always draft the matching archetype" odds past what the user wanted — the two are coupled through the same skill constants unless the base odds table also moves. If asked to retune bogey-free probability again, reach for **both** levers (skill-tier ranges *and* the unmatched odds table) rather than just cranking skill further, and verify with a real Monte Carlo sweep against the live content files (not the single-hole fixture test) before landing on numbers — see #38 for the exact methodology and final constants.
- **`countries.json` player skills/archetypes get edited by the user directly, outside sessions, from time to time** (happened again going into #38, as it did once before) — if the file shows as modified at session start with no corresponding conversation history, that's expected, not a mystery to investigate.
